<% content_for :title, "Showing competition" %>
<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>
  <div class="flex justify-between items-center mb-8">
    <div class="flex space-x-5 items-center">
      <h1 class="font-bold text-4xl"><%= @competition.name %></h1>
      <p class="text-gray-600 mt-2">开始日期: <%= @competition.start_date.strftime("%Y-%m-%d") %></p>
    </div>
    <div>
      <%= link_to "编辑", edit_competition_path(@competition), class: "btn mr-2" %>
      <%= link_to "返回", competitions_path, class: "btn" %>
    </div>
  </div>
  <div>
    <p class="text-gray-600">没有设定好的运动会项目，请点击下方按钮开始配置</p>
    <%= link_to "开始配置", competition_path(@competition), class: "btn btn-primary mt-4" %>
  </div>
  <%# <h2 class="font-bold text-2xl mb-4">设置流程</h2>
  <div class="flex justify-center my-8">
    <ul class="steps">
      <li class="step step-primary">添加项目</li>
      <li class="step">运动员报名</li>
      <li class="step">号码分配</li>
      <li class="step">日程设定</li>
      <li class="step">生成报告</li>
    </ul>
  </div> %>
</div>
